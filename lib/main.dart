import 'package:flutter/material.dart';
import 'maincode.dart';
main()
{
  runApp(MyApp());
}

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return mycode(
      
    );
  }
}
